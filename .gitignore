# Claude Code session metadata
.claude/

# Runtime files (heartbeat + watchdog log + watchdog pid, no van al repo)
brawlmacro_heartbeat.txt
brawlmacro_watchdog.log
brawlmacro_watchdog.pid
brawlmacro_historial.log

# Stats migradas a config.ini en v27.9.2 — el archivo viejo no va al repo
brawlmacro_stats.ini

# Datos personales del usuario (no comparten en repo público)
brawlmacro_config.ini
brawlmacro_eggs.txt

# Grafo de llamadas generado por el botón 🕸 (artefacto local, se regenera)
brawlmacro_callgraph.html

# Carpetas de mockups generados (pesadas, locales)
mockups/
mockups_png/

# Cache de Python
__pycache__/
*.pyc

# PPTX generado (es output, no source)
brawlmacro_mockups.pptx

# Artefactos de compilación de BrawlMacroCS (.NET/C#)
BrawlMacroCS/**/bin/
BrawlMacroCS/**/obj/
